// You can create a prefix variable if you want.
export const prefix = '/'

// You can also have commands for your bot like this.
export const commands = {
    'help': 'Displays all the commands',
    'hi': 'Says hi',
    'react': 'Reacts to the message with a random emoji',
} as Record<string, string>